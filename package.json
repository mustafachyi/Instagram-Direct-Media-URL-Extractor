{
  "name": "instagram-direct-media-URL-extractor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun"
  },
  "dependencies": {
    "hono": "4.8.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
